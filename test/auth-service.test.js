describe("auth service", () => {
	describe("create user", () => {
		it("adds new user to user table if does not already exist", () => {});
		it("returns already exists if the user already exists", () => {});
	});
	describe("find user by name", () => {
		it("returns matching name", () => {});
		it("returns null if cannot fins a match", () => {});
	});
	describe("find user by id", () => {
		it("deletes a ticker to the users portfolio given user and ticker", () => {});
		it("returns null if cannot find a match", () => {});
	});
});
